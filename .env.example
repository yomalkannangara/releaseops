POSTGRES_DB=releaseops
POSTGRES_USER=releaseops
POSTGRES_PASSWORD=replace-with-a-secure-password
POSTGRES_PORT=5433

BACKEND_PORT=8080

# Generate using: openssl rand -base64 32
JWT_SECRET=replace-with-a-generated-base64-secret
JWT_EXPIRATION_MS=3600000